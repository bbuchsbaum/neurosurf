# Geodesic Distance Utilities

Core helpers for vertex- and parcel-level geodesic computations on
neurosurf surfaces. Distances are computed on the surface graph using
weighted shortest paths (Dijkstra). Results can be chunked to control
memory and optionally cached for repeated queries.

## Value

NULL (documentation block only)
