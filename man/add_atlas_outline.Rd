% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/surface_plot.R
\name{add_atlas_outline}
\alias{add_atlas_outline}
\title{Add an atlas outline layer to a surface plot}
\usage{
add_atlas_outline(
  x,
  labels,
  rois = NULL,
  label = "atlas",
  outline_col = "black",
  outline_lwd = 1.5,
  outline_offset = 0.5,
  outline_halo = TRUE,
  outline_halo_col = "white",
  outline_halo_lwd = NULL,
  outline_lty = c("solid", "dashed"),
  ...
)
}
\arguments{
\item{x}{A \code{"neurosurf_plot"} object created by
\code{\link{surface_plot}}.}

\item{labels}{Numeric vector or list of vectors containing ROI labels for
each vertex. If a single vector is supplied, it is split across
hemispheres based on vertex counts.}

\item{rois}{Optional numeric vector of ROI ids to outline. If \code{NULL},
all ROIs present in \code{labels} are outlined.}

\item{label}{Optional character label for this outline layer.}

\item{outline_col}{Colour to use for ROI boundaries. Defaults to \code{"black"}.}

\item{outline_lwd}{Numeric line width for boundaries. Defaults to \code{1.5}.}

\item{outline_offset}{Numeric depth offset along surface normals to avoid
z-fighting. Defaults to \code{0.5}.}

\item{outline_halo}{Logical; if \code{TRUE}, draws a thicker halo under the
main line for better visibility. Defaults to \code{TRUE}.}

\item{outline_halo_col}{Colour for the halo. Defaults to \code{"white"}.}

\item{outline_halo_lwd}{Numeric line width for the halo. If \code{NULL},
slightly larger than \code{outline_lwd}.}

\item{outline_lty}{Line type for boundaries: \code{"solid"} or \code{"dashed"}.}

\item{...}{Additional arguments passed through to
\code{\link{add_surface_layer}}, allowing fine control over line colour,
width, offset, and halo appearance.}
}
\value{
A modified \code{"neurosurf_plot"} object.
}
\description{
This helper adds an outline-only layer to an existing \code{"neurosurf_plot"}
object using ROI labels. It configures sensible defaults for boundary
aesthetics (including a light halo and a small depth offset) so that atlas
outlines remain legible over filled statistical maps.
}
\examples{
\donttest{
fs <- load_fsaverage_std8("inflated")
p  <- surface_plot(fs$lh, fs$rh)
# roi_labels <- ... # per-vertex ROI ids
# p <- add_atlas_outline(p, roi_labels)
}

}
