% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_surface.R
\name{curv_cols_smooth}
\alias{curv_cols_smooth}
\title{Convert Curvature Values to Smooth Gradient Colors}
\usage{
curv_cols_smooth(
  vals,
  light = "#D4D4D4",
  dark = "#3A3A3A",
  quantiles = c(0.05, 0.95)
)
}
\arguments{
\item{vals}{A numeric vector of curvature values for each vertex.}

\item{light}{Hex color for the lightest shade (gyral crowns).
Default \code{"#D4D4D4"}.}

\item{dark}{Hex color for the darkest shade (sulcal fundi).
Default \code{"#3A3A3A"}.}

\item{quantiles}{Length-2 numeric vector of lower and upper quantiles used
to clamp extreme values before rescaling.  Default \code{c(0.05, 0.95)}.}
}
\value{
A character vector of hex color codes the same length as \code{vals}.
}
\description{
Maps surface curvature values to a continuous grayscale gradient, producing
a FreeSurfer-style sulcal shading that is visually smoother than the binary
mapping of \code{\link{curv_cols}}.
}
\details{
Values are clamped to the range defined by \code{quantiles} to prevent
outliers from washing out the colour map.
The clamped values are linearly interpolated between \code{dark} (most
negative / sulcal) and \code{light} (most positive / gyral).
}
\examples{
set.seed(1)
curv <- rnorm(500, sd = 0.1)
cols <- curv_cols_smooth(curv)
head(cols)

}
\seealso{
\code{\link{curv_cols}}, \code{\link{curvature}}, \code{\link{view_surface}}
}
