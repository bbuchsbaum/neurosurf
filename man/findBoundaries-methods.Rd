% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all_generic.R, R/roi_boundaries.R
\name{findBoundaries}
\alias{findBoundaries}
\alias{findBoundaries,NeuroSurface-method}
\title{Find Boundaries Between Regions on a Surface}
\usage{
findBoundaries(x, method = "midpoint", ...)

\S4method{findBoundaries}{NeuroSurface}(x, method = c("edge_vertices", "faces"), ...)
}
\arguments{
\item{x}{A \code{\linkS4class{NeuroSurface}} object whose \code{data} slot
contains integer ROI labels for each vertex.}

\item{method}{Boundary method passed to \code{\link{find_roi_boundaries}}.
Currently one of \code{"faces"} or \code{"edge_vertices"}.}

\item{...}{Additional arguments passed to \code{\link{find_roi_boundaries}}.}
}
\value{
An object containing boundary information. The specific structure depends
on the method implementation.

A list as returned by \code{\link{find_roi_boundaries}}.
}
\description{
This generic function identifies boundaries between different regions or parcellations
on a surface. The implementation depends on the class of the input object.
}
\details{
This function provides a high-level interface for finding boundaries between
different regions on a surface mesh. It typically returns coordinates and metadata
describing the boundaries between regions.
}
\examples{
\donttest{
# Requires surface with ROI labels
# boundaries <- findBoundaries(labeled_surface)
}

}
\seealso{
\code{\link{find_roi_boundaries}}

\code{\link{find_roi_boundaries}}
}
