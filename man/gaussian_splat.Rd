% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gaussian_splat.R
\name{gaussian_splat}
\alias{gaussian_splat}
\alias{gaussian_splat_vertex}
\alias{gaussian_splat_multi}
\title{Gaussian splats on surface meshes}
\usage{
gaussian_splat(surface, center, sigma, use_geodesic = FALSE)

gaussian_splat_vertex(surface, vertex_idx, sigma, use_geodesic = FALSE)

gaussian_splat_multi(
  surface,
  centers,
  sigmas,
  weights = NULL,
  use_geodesic = FALSE
)
}
\arguments{
\item{surface}{A \code{SurfaceGeometry}.}

\item{center}{Numeric vector of length 3 giving the centre coordinate.}

\item{sigma}{Positive numeric standard deviation of the Gaussian kernel.}

\item{use_geodesic}{Logical; when \code{TRUE} use along-surface distances.}

\item{vertex_idx}{Integer index (1-based) of the centre vertex.}

\item{centers}{Numeric matrix with three columns; each row is a centre.}

\item{sigmas}{Numeric vector of length 1 or \code{nrow(centers)}.}

\item{weights}{Optional numeric vector of length 1 or
\code{nrow(centers)} used to weight each centre (defaults to 1).}
}
\value{
A \code{NeuroSurface} with values
  \eqn{\exp(-d^2 / (2 * sigma^2))} at every vertex. For
  \code{gaussian_splat_multi} the per-centre maps are summed (after
  weighting).
}
\description{
Create per-vertex Gaussian falloff maps centred on coordinates or
vertices. Distances can be measured either in straight-line Euclidean
space or along the mesh using geodesic paths (Dijkstra on edge lengths).
}
\examples{
geom <- example_surface_geometry()
g1 <- gaussian_splat(geom, center = c(0, 0, 0), sigma = 1)
v_idx <- find_nearest_vertex(geom, c(0.1, 0, 0))
g2 <- gaussian_splat_vertex(geom, vertex_idx = v_idx, sigma = 1)
g_multi <- gaussian_splat_multi(
  geom,
  centers = rbind(c(0, 0, 0), c(1, 0, 0)),
  sigmas = c(0.5, 1)
)
}
