% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot_surface.R
\name{snapshot_surface}
\alias{snapshot_surface}
\title{Snapshot a surface to a PNG}
\usage{
snapshot_surface(surfgeom, file = NULL, width = 1200, height = 900, ...)
}
\arguments{
\item{surfgeom}{A \code{\linkS4class{SurfaceGeometry}} object.}

\item{file}{Output path for the PNG. Defaults to the current knitr figure
path when knitting, otherwise a temporary file.}

\item{width, height}{Device size in pixels (controls render resolution).}

\item{...}{Additional arguments passed to \code{\link{view_surface}}.}
}
\value{
The file path (invisibly). Callers can use
  \code{knitr::include_graphics()} or read the image via \code{png::readPNG()}.
  In headless mode without \code{webshot2}, an empty character vector is returned.
}
\description{
Convenience helper for vignettes and reports: renders a surface with
\code{view_surface()} onto an off-screen rgl device and saves a PNG.
When \code{rgl.useNULL()} is \code{TRUE} (headless builds), a proper snapshot
requires the \code{webshot2} package; otherwise a blank image is likely and an
empty path is returned. For static output that does not depend on a GL
session, use \code{\link{render_surface_rgba}} or
\code{\link{surface_figure}}.
}
\examples{
\donttest{
fs <- load_fsaverage_std8("inflated")
if (interactive()) {
  img <- snapshot_surface(fs$lh, viewpoint = "lateral", specular = "black")
}
}
}
